using System;

namespace _03CodeStartAndReading
{
    class Program
    {
        // 코드가 실행된다고 함은
        // 어디서부터인가
        // 한줄한줄씩 os가 내프로그램을
        // 읽어주는 것이다.

        //쪼개서 읽지 말아라

        static void Main(string[] args)
        {
            //한줄한줄 읽는다는  것이 좀 힘든일이지만
            // 그걸 ide가 도와준다.
            // 디버깅중 코드 한줄씩 실행
            // f10 으로 디버깅
            int a1 = 0;
            int a2 = 0;
            int a3 = 0;
            int a4 = 0;
            int a5 = 0;
            Console.WriteLine("하하");
        }
    }
}
